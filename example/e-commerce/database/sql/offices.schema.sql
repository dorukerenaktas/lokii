CREATE TABLE IF NOT EXISTS offices
  (
     office_code   INTEGER,
     city          VARCHAR,
     phone         VARCHAR,
     address_line1 VARCHAR,
     address_line2 VARCHAR,
     state         VARCHAR,
     country       VARCHAR,
     postal_code   VARCHAR,
     territory     VARCHAR
  );