CREATE TABLE IF NOT EXISTS employees
  (
     employee_number INTEGER,
     first_name      VARCHAR,
     last_name       VARCHAR,
     extension       VARCHAR,
     email           VARCHAR,
     office_code     INTEGER,
     reports_to      VARCHAR,
     job_title       VARCHAR
  );